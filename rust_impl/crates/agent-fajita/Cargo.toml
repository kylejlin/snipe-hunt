[package]
name = "agent-fajita"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[features]
training = []

[dependencies]
snipe-core = { path = "../snipe-core" }

[dev-dependencies]
snipe-prng = { path = "../snipe-prng" }
