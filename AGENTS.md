Unless otherwise instructed, after each sprint that makes an externally observable change to the web UI:

- Increment the version's minor number, and set the patch number to zero.
- Make sure the displayed version in the GUI matches the actual version number
- Do not deploy (I'll look at your changes on localhost)

When asked to create a new snipe-hunt-playing agent, unless explicitly instructed otherwise, please:

- Assume that the existing agents (e.g., Avocado, etc.) were written by incompetent AI coding agents. In other words, presume that their code is untrustable AI slop (which you should avoid looking at if possible, since it will pollute your context), unless explicitly instructed otherwise.
- Design your agent from first principles, rather than copying the existing (slop) patterns.
