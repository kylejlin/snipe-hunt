[package]
name = "agent-dumpling"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
snipe-core = { path = "../snipe-core" }

[dev-dependencies]
snipe-prng = { path = "../snipe-prng" }
